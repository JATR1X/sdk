<div class="sidebar-row">
    <h6>{__("search")}</h6>

    <form action="{""|fn_url}" name="landing_pages_search_form" method="get" class="{$form_meta}">
        {if $put_request_vars}
            {array_to_fields data=$smarty.request skip=["callback"]}
        {/if}

        {capture name="simple_search"}
            {$extra nofilter}

            <div class="sidebar-field">
                <label>{__("find_results_with")}</label>
                <input type="text" name="q" size="20" value="{$search.q}" />
            </div>

            {if !$landing_page_types}
                {assign var="landing_page_types" value=""|fn_get_current_landing_page_types}
            {/if}

            {if $landing_page_types|sizeof > 1}
                <div class="sidebar-field">
                    <label>{__("filling_type")}</label>
                    <select class="small" name="landing_page_type">
                        <option value="">- {__("select_filling_type")} -</option>
                        {foreach from=$landing_page_types item="p"}
                            <option value="{$p}" {if $search.landing_page_type == $p}selected="selected"{/if}>{__($p)}</option>
                        {/foreach}
                    </select>
                </div>
            {elseif $landing_page_types|is_array}
                <input type="hidden" name="landing_page_type" value="{$landing_page_types|reset}" />
            {else}
                <input type="hidden" name="landing_page_type" value="" />
            {/if}

            <div class="sidebar-field">
                <label>{__("status")}</label>
                <select class="small" name="status">
                    <option value="">- {__("select_status")} -</option>
                    <option value="A" {if $search.status == "A"}selected="selected"{/if}>{__("active")}</option>
                    <option value="D" {if $search.status == "D"}selected="selected"{/if}>{__("disabled")}</option>
                </select>
            </div>
        {/capture}

        {include file="common/advanced_search.tpl" simple_search=$smarty.capture.simple_search dispatch=$dispatch view_type=$view_type in_popup=true}
    </form>
</div>

<hr>
